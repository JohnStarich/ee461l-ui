import { getMovie } from 'moviematcher/helpers/get-movie';
import { module, test } from 'qunit';

module('Unit | Helper | get movie');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getMovie([42]);
  assert.ok(result);
});
